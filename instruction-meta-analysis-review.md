# 大量指示ログのメタ分析は、どこまで有効か — 実施レビュー

**実施日**：2026-06-30 ／ **対象**：marp-ChibaU への過去の指示（Claude Codeチャット履歴）と実デッキ・git履歴
**目的**：「自分が大量に出した指示」を分析して、最初から良いデザイン・表現になるよう CLAUDE.md / skill / CSS を直す

> この文書は「分析の結果」ではなく、**分析という手法自体がどれくらい使えるか**の振り返りです。
> 結果そのものは別途 [marp実運用デザイン実態のメモリ] と CLAUDE.md/skill/CSS の差分を参照。

---

## 0. 結論（先に一言）

**うまくいく。ただし「条件付き」**。
支配的な傾向・構造的な問題・繰り返す失敗の抽出には**非常に強い**。一方で「ユーザーの好み」「具体的な数値」を当てるのは**そのままでは危なく**、数値での裏取り・敵対的検証・実物確認を重ねて初めて信頼できる。
今回はその工程を踏んだので、**検証可能な改善（適用＋レンダリング確認済）にまで落ちた**。

---

## 1. 何をやったか（方法）

| 項目 | 規模 |
|---|---|
| 指示ログ | 229MB のチャット履歴(29セッション) → **タイプされた指示だけ 324件・約25kトークンに抽出** |
| git | 69コミットの設計進化（CSS・CLAUDE.md の差分） |
| 実デッキ | 直近 36デッキ（全132デックから）を1デック1エージェントで指紋採取 |
| エージェント | **計56体**（採掘5レンズ＋デッキ census 43＋統合・推奨・敵対的検証） |
| 人手の裏取り | 主要主張は私が直接 grep・レンダリングで再確認 |

工程：**抽出 → 多レンズ採掘 → 統合 → 改訂案 → 敵対的検証 → 人手で再検証 → 適用 → レンダリング目視**。

---

## 2. どれくらい「効いた」か（成果の証拠）

メタ分析が空論で終わらなかった証拠：

- **検証可能な改善に落ちた**：運用ルール（必須レンダリング確認・出典裏取り 等）、CSS正本の1本化、実態テーマ chiba-deck の文書化、受容デフォルトのCSS化、縦横の配分システムを実装し、**レンダリング目視で退行なしを確認**。
- **潜在バグを発見**：スキルCSSの3コピー乖離（home コピーが `--header-h:50px` で**56pxロック違反**）。これは指示ログには一切出てこない、静かな事故源だった。
- **「最初から良くなる」方向に直結**：「大きく」「上下中央に」「全幅下線に」等の頻出指示の多くが、既定値・1語クラスで不要化。

---

## 3. 強かったところ

1. **支配的カテゴリの特定**：最頻指示は 画像/ロゴ差替31・ビルド27・拡大25(縮小7)・takeaway文言25・位置23…（私が直接カウント）。体感でなく**実数で順位づけ**できる。
2. **単一指示では見えない構造的発見**：実運用の基盤テーマが `academic` でなく **`chiba-deck`（109 vs 29デック・私が直接確認）**、表紙は `cover-hero`（164 vs 8）。「1個1個の指示」を見ても絶対に出てこない、**全体を集計して初めて分かる**事実。
3. **繰り返す失敗モードの抽出**：「直したのに反映されてない」が最コスト、根因は「描画を見ずdiffで完了報告」。逐語証拠が複数セッションに散在。
4. **ドリフト・不整合の検出**：CSSコピーの乖離、ドキュメント内の不整合（本文23px vs 24px）。
5. **敵対的検証が実際に効いた**：下記4の罠を機械的に弾いた。

---

## 4. 弱かった／危なかったところ（ここが本題）

### ⚠️ 最大の罠：「出現回数」を「好み」と取り違える
デッキ横断のCSS出現回数（例：`pip-safe` 2382回、`font-weight:800` 5231回、`font-size` 上書き 16,719回、`--hdr-left-w:21%` が132デック ※いずれもエージェント集計値）は、**ユーザーが望んだ証拠ではなく、Claude自身が生成した定型（ボイラープレート）の証拠**であることが多い。
実際、敵対的検証エージェントは「これらは生成定型由来。タイプされた指示ログには pip/ワイプの言及ゼロ、hex三点の指定ゼロ」と指摘し、いくつかの推奨を `grounded=false` に落とした。

→ **教訓：証拠には2層あり、信頼度が違う**。
- **「意図」の証拠**＝ユーザーが*タイプした*指示ログ（高信頼）
- **「状態」の証拠**＝出荷されたCSS・デッキ（de-facto実態には高信頼だが、*意図*の証拠としては低信頼。中身の多くは自動生成）

### その他の弱点
- **頻度の体感バンドが甘い**：エージェントの「very_high/high」は主観。**grepの実数で裏取り必須**。
- **値を「それっぽく」捏造する**：teal/navy の `-dark`/`-soft` 等、実在しない中間値を補完していた。
- **文脈の欠落**：「もう少し右に」「これに変えて」はログに残るが、*何を見て*そう言ったか（対象画像・崩れ）は残らない。意図推定は半分しかできない。
- **サンプル選択バイアス**：直近36デッキ中心。古いデッキは別傾向（accentの既定色など）。
- **一回限り vs 定着**の区別が、検証なしには付かない。

---

## 5. 知見タイプ別の信頼度ルーブリック

| 知見のタイプ | そのままの信頼度 | 必要な裏取り |
|---|---|---|
| 支配カテゴリ・頻度順位 | **高** | grep実数（容易） |
| 構造・テーマ・ドリフト | **高** | 直接 grep / ファイル確認 |
| 繰り返す失敗モード | 中〜高 | 逐語の複数事例 |
| 個別のデザイン値・「好み」 | 低〜中 | 逐語 or レンダリング |
| **出現回数由来の「好み」** | **低** | 既定変更は逐語の実需要がある時だけ |

---

## 6. うまくいかせるための条件（再現レシピ）

1. **まず抽出**：生ログ229MBは扱えない。タイプ指示だけ抜き出し25kトークンに（前処理が成否を分ける）。
2. **多レンズ＋敵対的検証**：採掘は角度を変えて並列、最後に「証拠が薄い推奨を refute する」批評役を必ず置く。
3. **数値で裏取り**：体感バンドは grep の実数で上書き。
4. **「意図」と「状態」を分離**：タイプ指示＝好みの正本、出荷物＝実態の正本。混同しない。
5. **load-bearing な主張は人手で再検証**：今回 chiba-deck 基盤説・CSSドリフトは私が直接 grep。
6. **既定値の変更は逐語の実需要がある時だけ**：出現回数だけでデフォルトを変えない（既存デックを壊す）。
7. **適用後は必ずレンダリングして目視**：CSSは静的に正しくても見た目で確認するまで「完了」としない。

---

## 7. このリポジトリでの総合評価

| 観点 | 評価 |
|---|---|
| 支配傾向・構造・失敗モードの抽出 | **A**（実数・実物で裏取りでき、確度が高い） |
| 受容デザイン・具体値の特定 | **B**（検証を重ねれば信頼できる。生のままはB-） |
| 出現回数を根拠にした「好み」推定 | **C**（要注意。敵対的検証で弾く前提） |
| 改善への落とし込み（適用・検証込み） | **A**（検証可能な変更と潜在バグ発見に到達） |

**総括**：大量指示のメタ分析は、「何が多いか・何が壊れているか・何を繰り返し直しているか」を掴むのに**極めて有効**。ただし出力をそのまま信じると「自動生成の定型」を「あなたの好み」と取り違える。**数値の裏取り・敵対的検証・実物確認**という安全装置を付ければ、**確度高く“最初から良くする”改善に変換できる**——今回はそれが実証できた。
